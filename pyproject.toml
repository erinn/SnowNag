[project]
name = ""
version = "0.0.1"
requires-python = ">=3.14"
dependencies = ["requests"]

[dependency-groups]
dev = [
    "ipython",
    "pylint",
]

[tool.uv]
package = false
default-groups = "all"

[[tool.uv.index]]
name = "pypi"
url = "https://pypi.org/simple"
